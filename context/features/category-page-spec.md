# Category Page

## Overview

Get category page.

## Requirements

- In the category-home component read the category id. The category id is in the URL. It is formatted like this /:categoryId-categoryName
- Call the API /category/:categoryId/forum to get the category data and its forums. Put the data in a signal

## Notes

- Do not modify the HTML and SCSS, I will do this
- Do not store the API returned data into the store, use signals instead
