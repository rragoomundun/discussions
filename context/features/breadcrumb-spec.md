# Breadcrumb

## Overview

Breadcrumb component.

## Requirements

- Create a shared component called breadcrumb
- The component have an input that is an array of object with the following format: `[{link, title}]`
- By default the component always show the home page item. Add an home icon for this link
- After the home page item it shows the other items separated by a ">"
